<%= form_for(@user_subject) do |f| %>
  <% if @user_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_subject.errors.count, "error") %> prohibited this user_subject from being saved:</h2>

      <ul>
      <% @user_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.number_field :subject_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
